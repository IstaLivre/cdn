server {
    listen  <%= ENV['OPENSHIFT_NGINX_IP'] %>:<%= ENV['OPENSHIFT_NGINX_PORT'] %>;
    root    <%= ENV['OPENSHIFT_REPO_DIR'] %>public;

    index index.html;
    location / {
        try_files $uri $uri/;
    }
    location /static {
        try_files $uri $uri/;
    }
    
}
